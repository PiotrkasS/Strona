export default function Header() {
  return (
    <header className="header">
      <span className="header-logo">🎭</span>
      <span className="header-title">Panel Test Runner</span>
      <span className="header-subtitle">Playwright · PHP · React</span>
    </header>
  );
}
