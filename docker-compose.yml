services:
  app:
    build: .
    ports:
      - "8080:8000"
    environment:
      BASE_URL: ${BASE_URL:-http://localhost}
      PANEL_USER: ${PANEL_USER:-admin}
      PANEL_PASSWORD: ${PANEL_PASSWORD:-admin123}
    volumes:
      - ./tests:/app/tests:ro
